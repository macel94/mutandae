apiVersion: v1
kind: Service
metadata:
  name: mutandae
  labels:
    app.kubernetes.io/name: mutandae
    belacca.com/project: mutandae
spec:
  selector:
    app.kubernetes.io/name: mutandae
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
